# View the subsetted data (optional)
head(filtered_data)
# Export the filtered_nonlocalized_data to a TSV file
write.table(filtered_data, "TiO_GA_localized_precursor.tsv", sep = "\t", row.names = FALSE, quote = FALSE)
# Initialize an empty list to store the results
results <- list()
# Loop over each "TotalQuantity" column in filtered_nonlocalized_data
for (col in total_quantity_columns_filtered) {
# Get the column values
column_values <- filtered_nonlocalized_data[[col]]
# Filter out rows with 0, 1, or "Filtered"
valid_rows <- !(column_values %in% c(0, 1, "Filtered"))
# Filtered data based on valid rows
filtered_data <- filtered_nonlocalized_data[valid_rows, ]
# Total number of valid rows (excluding 0, 1, and "Filtered")
total_rows <- nrow(filtered_data)
# Count of phospho rows in EG.PrecursorId after filtering valid rows
phospho_rows <- sum(grepl("Phospho", filtered_data$EG.PrecursorId))
# Percent of Phospho rows out of total valid rows
percent_phospho_rows <- (phospho_rows / total_rows) * 100
# Total abundance (sum of values in the column, excluding invalid rows)
total_abundance <- sum(as.numeric(column_values[valid_rows]), na.rm = TRUE)
# Get the phospho values from the filtered data
phospho_column_values <- column_values[valid_rows & grepl("Phospho", filtered_data$EG.PrecursorId)]
# Total phospho abundance (sum of phospho values)
phospho_abundance <- sum(as.numeric(phospho_column_values), na.rm = TRUE)
# Percent of Phospho abundance out of total abundance
percent_phospho_abundance <- ifelse(total_abundance != 0, (phospho_abundance / total_abundance) * 100, 0)
# Store the results in the list
results[[col]] <- list(
Total_Rows = total_rows,
Phospho_Rows = phospho_rows,
Percent_Phospho_Rows = round(percent_phospho_rows, 2),
Total_Abundance = total_abundance,
Phospho_Abundance = phospho_abundance,
Percent_Phospho_Abundance = round(percent_phospho_abundance, 2)
)
}
# Convert the list into a data frame
results_df <- do.call(rbind, lapply(results, data.frame, stringsAsFactors = FALSE))
# Save the results to a CSV file
write.csv(results_df, "phospho_summary.csv", row.names = TRUE)
# Export the filtered_nonlocalized_data to a TSV file
write.table(filtered_nonlocalized_data, "filtered_nonlocalized_data.tsv", sep = "\t", row.names = FALSE, quote = FALSE)
write.table(filtered_data, "filtered_data.tsv", sep = "\t", row.names = FALSE, quote = FALSE)
# Save the results to a CSV file
write.csv(results_df, "GA_phospho_summary.csv", row.names = TRUE)
# Step 1: Identify columns with "SPQC" in the name and "EG.TotalQuantity" in the header
spqc_columns <- grep("6010", colnames(filtered_nonlocalized_data), value = TRUE)
total_quantity_columns <- grep("EG.TotalQuantity", colnames(filtered_nonlocalized_data), value = TRUE)
# Combine these two sets of columns (i.e., columns that contain both "SPQC" and "EG.TotalQuantity")
target_columns <- intersect(spqc_columns, total_quantity_columns)
# Step 2: Remove rows where any of the target columns have 0, 1, or "Filtered"
valid_rows <- !apply(filtered_nonlocalized_data[target_columns], 1, function(x) any(x %in% c(0, 1, "Filtered")))
# Step 3: Keep only rows where "EG.PrecursorId" contains "Phospho"
phospho_rows <- grepl("Phospho", filtered_nonlocalized_data$EG.PrecursorId)
# Combine the conditions: valid rows and phospho rows
filtered_data <- filtered_nonlocalized_data[valid_rows & phospho_rows, ]
# View the filtered data (optional)
head(filtered_data)
# Step 1: Identify the columns to keep
columns_to_keep <- c("PG.Genes","Combined_Protein_Info","EG.PrecursorId",grep("EG.TotalQuantity", colnames(filtered_data), value = TRUE))
# Step 2: Subset the filtered_data to keep only the identified columns
filtered_data <- filtered_data[, columns_to_keep]
# View the subsetted data (optional)
head(filtered_data)
# Export the filtered_nonlocalized_data to a TSV file
write.table(filtered_data, "TiO_GA_localized_precursor.tsv", sep = "\t", row.names = FALSE, quote = FALSE)
# Loading packages
library(readr)
library(dplyr)
# Import the nonlocalized TSV file
nonlocalized_data <- "20250524_004227_6010_phos_Glu_Report_precursor_noloc.tsv"
nonlocalized_data <- read_tsv(nonlocalized_data)
# Importing the second TSV file
localized_data <- "20250524_004227_6010_phos_Glu_Report_precursor_loc.tsv"
localized_data <- read_tsv(localized_data)
# View the first few rows of both datasets
head(nonlocalized_data)
head(localized_data)
# Loading packages
library(readr)
library(dplyr)
# Import the nonlocalized TSV file
nonlocalized_data <- "20250524_004227_6010_phos_Glu_Report_precursor_noloc.tsv"
nonlocalized_data <- read_tsv(nonlocalized_data)
# Importing the second TSV file
localized_data <- "20250524_004227_6010_phos_Glu_Report_precursor_loc.tsv"
localized_data <- read_tsv(localized_data)
# View the first few rows of both datasets
head(nonlocalized_data)
head(localized_data)
# Step 1: Extract the two characters following the underscore in EG.PrecursorId
nonlocalized_data$PrecursorSuffix <- sub(".*_(..)$", "\\1", nonlocalized_data$EG.PrecursorId)
# Step 2: Combine PG.ProteinNames, EG.ProteinPTMLocations, and the two characters from EG.PrecursorId
nonlocalized_data$Combined_Protein_Info <- paste(nonlocalized_data$PG.ProteinNames,
nonlocalized_data$EG.ProteinPTMLocations,
nonlocalized_data$PrecursorSuffix,
sep = "_")
# Step 3: Reorder columns to make Combined_Protein_Info the first column
nonlocalized_data <- nonlocalized_data %>%
select(Combined_Protein_Info, everything())
# Step 4: View the new data frame with the combined column as the first column
head(nonlocalized_data)
# Filter rows where at least two "PTMLocalizationProbabilities" columns are not "Filtered"
filtered_localized_data <- localized_data %>%
filter(rowSums(select(., contains("PTMLocalizationProbabilities")) != "Filtered", na.rm = TRUE) >= 1)
# View the filtered dataset
head(filtered_localized_data)
# Assuming both data frames have a common column to join on, e.g., "id"
filtered_nonlocalized_data <- nonlocalized_data %>%
filter(EG.PrecursorId %in% filtered_localized_data$EG.PrecursorId)
# View the filtered data
head(filtered_nonlocalized_data)
# Identify columns that contain "TotalQuantity" in the column name for filtered_nonlocalized_data
total_quantity_columns_filtered <- grep("TotalQuantity", colnames(filtered_nonlocalized_data), value = TRUE)
# View the columns found in filtered_nonlocalized_data
total_quantity_columns_filtered
# Loop over each "TotalQuantity" column in filtered_nonlocalized_data
for (col in total_quantity_columns_filtered) {
# Get the column values
column_values <- filtered_nonlocalized_data[[col]]
# Filter out rows with 0 or "Filtered"
valid_rows <- !(column_values %in% c('0', "Filtered"))
# Count the number of valid rows (excluding 0 and "Filtered")
valid_row_count <- sum(valid_rows)
# Calculate the sum of valid rows (excluding 0 and "Filtered")
valid_row_sum <- sum(as.numeric(column_values[valid_rows]), na.rm = TRUE)
# Print the results for the current column
cat("Column:", col, "\n")
cat("  Valid Row Count (excluding '0', 'Filtered'):", valid_row_count, "\n")
cat("  Sum of Valid Rows (excluding '0', 'Filtered'):", valid_row_sum, "\n\n")
}
# Filter rows where EG.PrecursorId contains "Phospho"
filtered_phospho_data <- filtered_nonlocalized_data[grepl("Phospho", filtered_nonlocalized_data$EG.PrecursorId), ]
# Loop over each "TotalQuantity" column in the filtered_phospho_data
for (col in total_quantity_columns_filtered) {
# Get the column values
column_values <- filtered_phospho_data[[col]]
# Filter out rows with 0, 1, or "Filtered"
valid_rows <- !(column_values %in% c(0, 1, "Filtered"))
# Count the number of valid rows (excluding 0, 1, and "Filtered")
valid_row_count <- sum(valid_rows)
# Calculate the sum of valid rows (excluding 0, 1, and "Filtered")
valid_row_sum <- sum(as.numeric(column_values[valid_rows]), na.rm = TRUE)
# Print the results for the current column
cat("Column:", col, "\n")
cat("  Valid Row Count (excluding 0, 1, 'Filtered') in Phospho rows:", valid_row_count, "\n")
cat("  Sum of Valid Rows (excluding 0, 1, 'Filtered') in Phospho rows:", valid_row_sum, "\n\n")
}
# Loop over each "TotalQuantity" column in the filtered_nonlocalized_data
for (col in total_quantity_columns_filtered) {
# Get the column values
column_values <- filtered_nonlocalized_data[[col]]
# Total number of rows in the full dataset
total_rows <- nrow(filtered_nonlocalized_data)
# Count of rows containing "Phospho" in EG.PrecursorId
phospho_rows <- sum(grepl("Phospho", filtered_nonlocalized_data$EG.PrecursorId))
# Percent of Phospho rows out of total rows
percent_phospho_rows <- (phospho_rows / total_rows) * 100
# Total abundance (sum of values in the column)
total_abundance <- sum(as.numeric(column_values), na.rm = TRUE)
# Get the phospho values from rows where EG.PrecursorId contains "Phospho"
phospho_column_values <- column_values[grepl("Phospho", filtered_nonlocalized_data$EG.PrecursorId)]
# Total phospho abundance
total_phospho_abundance <- sum(as.numeric(phospho_column_values), na.rm = TRUE)
# Percent of Phospho abundance out of total abundance
percent_phospho_abundance <- (total_phospho_abundance / total_abundance) * 100
# Print the results for the current column
cat("Column:", col, "\n")
cat("  Percent of Phospho rows out of total rows:", round(percent_phospho_rows, 2), "%\n")
cat("  Percent of Phospho abundance out of total abundance:", round(percent_phospho_abundance, 2), "%\n\n")
}
# Initialize an empty list to store the results
results <- list()
# Loop over each "TotalQuantity" column in filtered_nonlocalized_data
for (col in total_quantity_columns_filtered) {
# Get the column values
column_values <- filtered_nonlocalized_data[[col]]
# Filter out rows with 0, 1, or "Filtered"
valid_rows <- !(column_values %in% c(0, 1, "Filtered"))
# Filtered data based on valid rows
filtered_data <- filtered_nonlocalized_data[valid_rows, ]
# Total number of valid rows (excluding 0, 1, and "Filtered")
total_rows <- nrow(filtered_data)
# Count of phospho rows in EG.PrecursorId after filtering valid rows
phospho_rows <- sum(grepl("Phospho", filtered_data$EG.PrecursorId))
# Percent of Phospho rows out of total valid rows
percent_phospho_rows <- (phospho_rows / total_rows) * 100
# Total abundance (sum of values in the column, excluding invalid rows)
total_abundance <- sum(as.numeric(column_values[valid_rows]), na.rm = TRUE)
# Get the phospho values from the filtered data
phospho_column_values <- column_values[valid_rows & grepl("Phospho", filtered_data$EG.PrecursorId)]
# Total phospho abundance (sum of phospho values)
phospho_abundance <- sum(as.numeric(phospho_column_values), na.rm = TRUE)
# Percent of Phospho abundance out of total abundance
percent_phospho_abundance <- ifelse(total_abundance != 0, (phospho_abundance / total_abundance) * 100, 0)
# Store the results in the list
results[[col]] <- list(
Total_Rows = total_rows,
Phospho_Rows = phospho_rows,
Percent_Phospho_Rows = round(percent_phospho_rows, 2),
Total_Abundance = total_abundance,
Phospho_Abundance = phospho_abundance,
Percent_Phospho_Abundance = round(percent_phospho_abundance, 2)
)
}
# Convert the list into a data frame
results_df <- do.call(rbind, lapply(results, data.frame, stringsAsFactors = FALSE))
# Save the results to a CSV file
write.csv(results_df, "Glu_phospho_summary.csv", row.names = TRUE)
# Export the filtered_nonlocalized_data to a TSV file
write.table(filtered_nonlocalized_data, "filtered_nonlocalized_data.tsv", sep = "\t", row.names = FALSE, quote = FALSE)
write.table(filtered_data, "filtered_data.tsv", sep = "\t", row.names = FALSE, quote = FALSE)
# Step 1: Identify columns with "SPQC" in the name and "EG.TotalQuantity" in the header
spqc_columns <- grep("6010", colnames(filtered_nonlocalized_data), value = TRUE)
total_quantity_columns <- grep("EG.TotalQuantity", colnames(filtered_nonlocalized_data), value = TRUE)
# Combine these two sets of columns (i.e., columns that contain both "SPQC" and "EG.TotalQuantity")
target_columns <- intersect(spqc_columns, total_quantity_columns)
# Step 2: Remove rows where any of the target columns have 0, 1, or "Filtered"
valid_rows <- !apply(filtered_nonlocalized_data[target_columns], 1, function(x) any(x %in% c(0, 1, "Filtered")))
# Step 3: Keep only rows where "EG.PrecursorId" contains "Phospho"
phospho_rows <- grepl("Phospho", filtered_nonlocalized_data$EG.PrecursorId)
# Combine the conditions: valid rows and phospho rows
filtered_data <- filtered_nonlocalized_data[valid_rows & phospho_rows, ]
# View the filtered data (optional)
head(filtered_data)
# Step 1: Identify the columns to keep
columns_to_keep <- c("PG.Genes","Combined_Protein_Info","EG.PrecursorId",grep("EG.TotalQuantity", colnames(filtered_data), value = TRUE))
# Step 2: Subset the filtered_data to keep only the identified columns
filtered_data <- filtered_data[, columns_to_keep]
# View the subsetted data (optional)
head(filtered_data)
# Export the filtered_nonlocalized_data to a TSV file
write.table(filtered_data, "TiO_Glu_localized_precursor_nomissing.tsv", sep = "\t", row.names = FALSE, quote = FALSE)
# Load necessary library
library(dplyr)
# List all text files in the working directory
files <- list.files(pattern = "\\.txt$")
# Function to process each file
process_file <- function(file) {
# Read the file, skipping the first 3 rows (so data start with row 4)
df <- read.delim(file, header = TRUE, skip = 3, stringsAsFactors = FALSE, check.names = FALSE)
# Rename the first column (Column A) to "Scan/Ion"
names(df)[1] <- "Scan/Ion"
# Extract the first 7 characters of the file name to use as a prefix
prefix <- substr(basename(file), 1, 7)
# Identify the "Total" and "%Total" columns and rename them by adding the prefix.
# (Adjust case if needed; here we assume the headers are exactly "Total" and "%Total")
current_names <- names(df)
idx_total <- which(current_names == "Total")
idx_pctTotal <- which(current_names == "%Total")
new_total <- paste0(prefix, "_Total")
new_pctTotal <- paste0(prefix, "_%Total")
if(length(idx_total) == 1) {
names(df)[idx_total] <- new_total
}
if(length(idx_pctTotal) == 1) {
names(df)[idx_pctTotal] <- new_pctTotal
}
# Select only the columns of interest: "Scan/Ion", and the newly renamed total columns.
df_subset <- df[, c("Scan/Ion", new_total, new_pctTotal)]
return(df_subset)
}
# Process each file and store the results in a list
data_list <- lapply(files, process_file)
# Merge all data frames by the "Scan/Ion" column
combined_df <- Reduce(function(x, y) merge(x, y, by = "Scan/Ion", all = TRUE), data_list)
# Convert the combined data frame to a matrix:
# Use the "Scan/Ion" column as row names and remove it from the matrix.
combined_matrix <- as.matrix(combined_df[,-1])
rownames(combined_matrix) <- combined_df$`Scan/Ion`
# Display the resulting matrix
combined_matrix
# A helper function to trim whitespace
trim <- function(x) {
gsub("^\\s+|\\s+$", "", x)
}
# Function to process one file
process_file <- function(file) {
# Read the entire file as lines and skip the first 3 lines (cells A1–A3)
lines <- readLines(file)
if(length(lines) <= 3) stop("File has too few lines!")
lines <- lines[-(1:3)]
# The very first line (A4) is assumed to be the header row.
header_line <- lines[1]
header_fields <- unlist(strsplit(header_line, "\t"))
# Force the first header to be "Scan/Ion"
header_fields[1] <- "Scan/Ion"
# List to collect processed rows; first row will be the header (we keep it as is)
processed_rows <- list(header_fields)
# Initialize current group as NA
current_group <- NA
# Process each subsequent line (preserving order)
for(i in 2:length(lines)) {
line <- lines[i]
# Check if this is a group header line: it starts with backslashes.
if(grepl("^\\\\+", line)) {
# Remove all backslashes and trim; if any text remains, treat it as the group name.
grp <- trim(gsub("\\\\", "", line))
if(nchar(grp) > 0) {
current_group <- grp
}
# Do not add the group header line itself to the data.
next
}
# (Optional) Skip repeated header rows (if the line equals the header_line after trimming)
if(trim(line) == trim(header_line)) next
# Split the line into fields by tab
fields <- unlist(strsplit(line, "\t"))
# If there are too few fields, skip the line.
if(length(fields) < 3) next
# For data rows (i.e. not the overall header), if a current group is set,
# prepend the group name to the first field (unless it already contains it).
if(!is.na(current_group)) {
if(!grepl(current_group, fields[1], ignore.case = TRUE)) {
fields[1] <- paste(current_group, fields[1], sep=": ")
}
}
processed_rows[[length(processed_rows) + 1]] <- fields
}
# To ensure every row has the same number of columns, pad shorter rows with NA.
max_cols <- max(sapply(processed_rows, length))
processed_rows <- lapply(processed_rows, function(x) { length(x) <- max_cols; x })
# Convert list of rows into a data frame.
df <- as.data.frame(do.call(rbind, processed_rows), stringsAsFactors = FALSE)
# Use the first row as header names and remove it from data.
colnames(df) <- df[1, ]
df <- df[-1, ]
# Keep only the columns "Scan/Ion", "Total" and "%Total"
# (Assumes these column names appear exactly; adjust if needed.)
cols_to_keep <- c("Scan/Ion", "Total", "%Total")
df <- df[, cols_to_keep, drop = FALSE]
# Extract a file prefix (first 7 characters of the file name)
prefix <- substr(basename(file), 1, 7)
# Rename the "Total" and "%Total" columns by prefixing them
colnames(df)[colnames(df) == "Total"] <- paste0(prefix, "_Total")
colnames(df)[colnames(df) == "%Total"] <- paste0(prefix, "_%Total")
return(df)
}
# List all text files (adjust pattern as needed)
files <- list.files(pattern = "\\.txt$")
# Process each file and store results in a list
data_list <- lapply(files, process_file)
# Combine data from multiple files.
# This code assumes that the "Scan/Ion" column (first column) is identical and in the same order.
if(length(data_list) > 0) {
combined_df <- data_list[[1]]
if(length(data_list) > 1) {
for(i in 2:length(data_list)) {
if(!all(combined_df$`Scan/Ion` == data_list[[i]]$`Scan/Ion`)) {
stop("The 'Scan/Ion' values do not match across files!")
}
# Append the numeric columns (drop the "Scan/Ion" column from subsequent files)
combined_df <- cbind(combined_df, data_list[[i]][, -1, drop = FALSE])
}
}
# Convert the combined data (excluding "Scan/Ion") to a matrix,
# using "Scan/Ion" as the row names.
combined_matrix <- as.matrix(combined_df[, -1, drop = FALSE])
rownames(combined_matrix) <- combined_df$`Scan/Ion`
# Display the resulting matrix
combined_matrix
}
# Load necessary library
library(dplyr)
# List all text files in the working directory
files <- list.files(pattern = "\\.txt$")
# Function to process each file
process_file <- function(file) {
# Read the file, skipping the first 3 rows (so data start with row 4)
df <- read.delim(file, header = TRUE, skip = 3, stringsAsFactors = FALSE, check.names = FALSE)
# Rename the first column (Column A) to "Scan/Ion"
names(df)[1] <- "Scan/Ion"
# Extract the first 7 characters of the file name to use as a prefix
prefix <- substr(basename(file), 1, 7)
# Identify the "Total" and "%Total" columns and rename them by adding the prefix.
# (Adjust case if needed; here we assume the headers are exactly "Total" and "%Total")
current_names <- names(df)
idx_total <- which(current_names == "Total")
idx_pctTotal <- which(current_names == "%Total")
new_total <- paste0(prefix, "_Total")
new_pctTotal <- paste0(prefix, "_%Total")
if(length(idx_total) == 1) {
names(df)[idx_total] <- new_total
}
if(length(idx_pctTotal) == 1) {
names(df)[idx_pctTotal] <- new_pctTotal
}
# Select only the columns of interest: "Scan/Ion", and the newly renamed total columns.
df_subset <- df[, c("Scan/Ion", new_total, new_pctTotal)]
return(df_subset)
}
# Process each file and store the results in a list
data_list <- lapply(files, process_file)
# Merge all data frames by the "Scan/Ion" column
combined_df <- Reduce(function(x, y) merge(x, y, by = "Scan/Ion", all = TRUE), data_list)
# Convert the combined data frame to a matrix:
# Use the "Scan/Ion" column as row names and remove it from the matrix.
combined_matrix <- as.matrix(combined_df[,-1])
rownames(combined_matrix) <- combined_df$`Scan/Ion`
# Display the resulting matrix
combined_matrix
# A helper function to trim whitespace
trim <- function(x) {
gsub("^\\s+|\\s+$", "", x)
}
# Function to process one file
process_file <- function(file) {
# Read all lines from the file and skip the first 3 lines
lines <- readLines(file)
if(length(lines) <= 3) stop("File has too few lines!")
lines <- lines[-(1:3)]
# The first line (A4) is assumed to be the header row.
header_line <- lines[1]
header_fields <- unlist(strsplit(header_line, "\t"))
# Rename the first header to "Scan/Ion"
header_fields[1] <- "Scan/Ion"
# List to store processed rows (first row is header)
processed_rows <- list(header_fields)
# Initialize current group as NA
current_group <- NA
# Process each subsequent line (preserving order)
for(i in 2:length(lines)) {
line <- lines[i]
# Check for group header rows (starting with backslashes)
if(grepl("^\\\\+", line)) {
# Remove backslashes and trim to obtain group name (e.g., "HexNAc")
grp <- trim(gsub("\\\\", "", line))
if(nchar(grp) > 0) {
current_group <- grp
}
# Do not add the group header line to data rows
next
}
# (Optional) Skip repeated header rows
if(trim(line) == trim(header_line)) next
# Split the line into fields using tab as delimiter
fields <- unlist(strsplit(line, "\t"))
# Skip if there are too few fields
if(length(fields) < 3) next
# Prepend the group name (if available) to the first field, unless already present
if(!is.na(current_group)) {
if(!grepl(current_group, fields[1], ignore.case = TRUE)) {
fields[1] <- paste(current_group, fields[1], sep=": ")
}
}
processed_rows[[length(processed_rows) + 1]] <- fields
}
# Pad rows to have the same number of columns
max_cols <- max(sapply(processed_rows, length))
processed_rows <- lapply(processed_rows, function(x) { length(x) <- max_cols; x })
# Convert the list of rows into a data frame
df <- as.data.frame(do.call(rbind, processed_rows), stringsAsFactors = FALSE)
# Use the first row as column names, then remove it from the data
colnames(df) <- df[1, ]
df <- df[-1, ]
# Keep only the columns "Scan/Ion", "Total", and "%Total"
cols_to_keep <- c("Scan/Ion", "Total", "%Total")
df <- df[, cols_to_keep, drop = FALSE]
# Extract the file prefix (first 7 characters of the file name)
prefix <- substr(basename(file), 1, 7)
# Rename the "Total" and "%Total" columns by adding the prefix
colnames(df)[colnames(df) == "Total"] <- paste0(prefix, "_Total")
colnames(df)[colnames(df) == "%Total"] <- paste0(prefix, "_%Total")
return(df)
}
# List all text files (adjust the pattern if needed)
files <- list.files(pattern = "\\.txt$")
# Process each file and store the results in a list
data_list <- lapply(files, process_file)
# Combine the data frames assuming that the "Scan/Ion" column is identical and in the same order.
if(length(data_list) > 0) {
combined_df <- data_list[[1]]
if(length(data_list) > 1) {
for(i in 2:length(data_list)) {
if(!all(combined_df$`Scan/Ion` == data_list[[i]]$`Scan/Ion`)) {
stop("The 'Scan/Ion' values do not match across files!")
}
# Append the numeric columns (excluding "Scan/Ion") from each subsequent file
combined_df <- cbind(combined_df, data_list[[i]][, -1, drop = FALSE])
}
}
## Group the numeric columns:
# Extract column names corresponding to the "Total" and "%Total" values
total_cols <- grep("_Total$", colnames(combined_df), value = TRUE)
pcttotal_cols <- grep("_%Total$", colnames(combined_df), value = TRUE)
# Reorder columns so that the first column remains "Scan/Ion",
# then all "Total" columns, followed by all "%Total" columns.
combined_df <- combined_df[, c("Scan/Ion", total_cols, pcttotal_cols)]
# Convert the numeric portion of the data frame into a matrix.
# Row names will be the "Scan/Ion" column.
combined_matrix <- as.matrix(combined_df[, -1, drop = FALSE])
rownames(combined_matrix) <- combined_df$`Scan/Ion`
# Display the combined data frame and the matrix.
print("Combined Data Frame:")
print(combined_df)
print("Combined Matrix (numeric columns only):")
print(combined_matrix)
}
write.csv(combined_df, "combined_Glycounter_results.csv", row.names = FALSE)
runApp('~/11034/app_9125_1.R')
runApp('~/11034/Shiny/app_9125_1.R')
runApp('~/11034/Shiny/app_92125_2.R')
runApp('~/11034/Shiny/app_92125_2.R')
runApp('~/11034/Shiny/app_91253.R')
runApp('~/11034/Shiny/app_090125_4.R')
ggVolcanoR::runApp()
runApp('~/11034/Shiny/app_090125_4.R')
runApp('~/11034/Shiny/app_090125_4.R')
runApp('~/11034/Shiny/app_090125_4.R')
runApp('~/11034/Shiny/app_090125_4.R')
